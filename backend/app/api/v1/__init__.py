"""Version 1 of the HTTP API.

Aggregates all v1 feature routers so :mod:`app.main` can mount a single router
under the configured API prefix. Add new endpoint modules here as the platform
grows.
"""
