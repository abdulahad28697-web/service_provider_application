# --- Core web framework ------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Data layer ---------------------------------------------------------
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.30.0        # PostgreSQL async driver (production / Docker)
aiosqlite>=0.20.0      # SQLite async driver (local unit tests)

# --- Validation / settings ----------------------------------------------
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0

# --- Security -----------------------------------------------------------
PyJWT>=2.8.0
bcrypt>=4.1.0

# --- Messaging ----------------------------------------------------------
redis>=5.0.0

# --- Tests / dev --------------------------------------------------------
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
