{
  "name": "service-provider-application",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"python -m uvicorn main:app --reload --app-dir backend --port 8000\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
